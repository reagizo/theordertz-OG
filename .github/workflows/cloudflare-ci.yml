name: Build and Deploy to Cloudflare

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      cf_env:
        description: 'Cloudflare environment to deploy to (staging|production)'
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '22'
      - run: npm ci
      - name: Build with secrets
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          VITE_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          CLOUDFLARE: 'true'
        run: npm run build
      - name: Save dist as artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist

  deploy_cf:
    needs: build
    if: ${{ github.event.inputs.cf_env || true }}
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event.inputs.cf_env || 'production' }}
      url: https://theordertz.reagizo.workers.dev
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '22'
      - name: Restore dist artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist
      - run: npm ci
      - name: Set Cloudflare secrets
        run: |
          npx wrangler secret put SUPABASE_URL --env ${{ github.event.inputs.cf_env || 'production' }} --name theordertz <<< '${{ secrets.SUPABASE_URL }}'
          npx wrangler secret put VITE_SUPABASE_ANON_KEY --env ${{ github.event.inputs.cf_env || 'production' }} --name theordertz <<< '${{ secrets.SUPABASE_ANON_KEY }}'
          npx wrangler secret put SUPABASE_SERVICE_ROLE_KEY --env ${{ github.event.inputs.cf_env || 'production' }} --name theordertz <<< '${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      - name: Deploy to Cloudflare
        env:
          CLOUDFLARE: 'true'
          CF_ENV: ${{ github.event.inputs.cf_env || 'production' }}
          SKIP_BUILD: 'true'
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: npm run deploy
